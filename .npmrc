# Enforce engine versions defined in package.json "engines" field.
# Prevents accidental use of incompatible Node or pnpm versions.
engine-strict=true

# Ensure the correct package manager is used (pnpm only).
# Running `npm install` or `yarn` will fail with a clear error.
only-allow-pnpm=true
