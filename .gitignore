# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
node_modules
dist

# pnpm temporary lockfile artifacts (leftover from interrupted installs)
# Format: pnpm-lock.yaml.{PID} — safe to delete, never commit
pnpm-lock.yaml.*
dist-ssr

# ─── Environment Files (SECURITY) ───────────────────────────────────────────
# Block ALL env files by default
.env
.env.local
.env.*.local
.env.production
.env.staging

# EXPLICITLY ALLOW .env.example — this file is safe to commit and required for onboarding
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

#Local files
Script_Database.md
# Testing coverage & reports
coverage/
vitest-report.json
test_output.txt
test*.json
*.log
.agent
api.json
BE-Document
.specify
specs
CLAUDE.md
GEMINI.md
